import { useState } from "react";
const Answer = (Calculation) => {    
    return (
        <div className="bg-amber-800 m-2 text-8xl font-bold text-center w-20">{Calculation}</div>
    );
};  

export default Answer